# Bruteforce tool.
A simple tool used to brute force a 4 digit pin on a web application. Can be modified to accept a list of usernames and/or passwords as needed.

# Scanner tool.
Basic tool used to scan websites for any SQL injection or XSS exploits.

# Reverse shell tool.
Basic script to start a reverse shell on a system vulnerable to command injections via XSS.
